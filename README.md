this is a skeleton project forcused on my understanding :
database: MySQL8.0-server(changed from sqlite3)
django pipenv shell *2 users created (one from admin panel(manager creation*wiped by db change) and one from admin panel check*also wiped *but present in old dbsqlite3 db file))
python manage.py runserver

note to observer : SKELETON PROJECT....ONLY BASIC DISPLAY(Besides DJANGO standard admin or mangmnt panel)
